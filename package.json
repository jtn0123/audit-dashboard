{
  "name": "audit-dashboard",
  "version": "2.1.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "test": "node --test tests/*.js",
    "test:coverage": "node --test --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=coverage/lcov.info --test-reporter=spec --test-reporter-destination=stdout tests/*.js",
    "lint": "eslint ."
  },
  "dependencies": {
    "express": "^4.22.2"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.0.2",
    "globals": "^17.9.0"
  }
}
